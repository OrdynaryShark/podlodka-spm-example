import XCTest
@testable import SPM_package_structure

final class SPM_package_structureTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SPM_package_structure().text, "Hello, World!")
    }
}
